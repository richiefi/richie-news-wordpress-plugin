# Plugin version request tracking

- Added version metadata headers to outgoing Richie server requests in both plugin trees.
- Kept existing request headers such as authorization and conditional cache headers intact.
- Added focused tests for request header helpers and cached request metadata.
